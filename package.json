{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "scripts": {
    "release": "lerna version",
    "publish": "lerna publish from-package --yes",
    "publish:rc": "lerna publish from-package --yes --canary --pre-dist-tag=rc --preid=rc"
  },
  "workspaces": [
    "packages/test-lerna-repo-a",
    "packages/test-lerna-repo-b"
  ],
  "npmClient": "npm"
}
